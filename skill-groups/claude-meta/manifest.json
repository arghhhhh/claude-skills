{
  "name": "claude-meta",
  "description": "Meta-utilities for working with Claude itself — slash commands and agents that query Claude's own model list, version info, etc.",
  "version": "0.4.1",
  "notes": "/export-proper and /import-proper moved to the claude-conversation-transfer group, which owns both the binary and the slash commands that drive it.",
  "prerequisites": [],
  "install": {
    "check": "true",
    "methods": [
      {
        "name": "noop",
        "platforms": ["macos", "linux", "windows"],
        "check": "true",
        "command": "true"
      }
    ],
    "notes": "No software dependencies — pure markdown commands and agents."
  },
  "test": {
    "command": "true",
    "expect_success": true
  },
  "skills": [],
  "agents": ["model-lister"],
  "commands": ["models-list"]
}
