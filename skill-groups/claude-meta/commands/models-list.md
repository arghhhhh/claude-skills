---
description: List all Claude models accepted by /model, grouped by tier (newest → deprecated)
allowed-tools: Agent
---

Launch the `model-lister` subagent (Agent tool, subagent_type: model-lister) with the prompt:

"List the current Claude models."

Display the subagent's response verbatim. Do not summarize, do not add commentary, do not reorder. The agent returns a complete formatted list — show it as-is.
