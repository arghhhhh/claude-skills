{
  "name": "ilspy",
  "description": "Agent-friendly .NET decompilation via ilspy-cli (akiselev/ghidra-cli, ilspy-cli crate) — type/method listing, single-method decompilation, regex search, assembly metadata, and .NET vs native detection using the ILSpy ICSharpCode.Decompiler engine.",
  "version": "0.1.0",
  "prerequisites": [
    "Rust 1.70+ (cargo on PATH, for source build)",
    ".NET 8 SDK (build) and .NET 8+ runtime (run)"
  ],
  "install": {
    "check": "ilspy --version",
    "methods": [
      {
        "name": "cargo-git",
        "platforms": ["windows", "macos", "linux"],
        "check": "cargo --version",
        "command": "cargo install --git https://github.com/akiselev/ghidra-cli ilspy-cli --locked"
      }
    ],
    "notes": "ilspy-cli is a subcrate of github.com/akiselev/ghidra-cli — pass the package name with `--git ... ilspy-cli`. The build script runs `dotnet publish` on the C# bridge (IlSpyBridge.dll). After install, copy `<repo>/ilspy-cli/target/release/bridge/` to a stable location and set `ILSPY_BRIDGE_DIR` so the bridge survives a `cargo clean`."
  },
  "test": {
    "command": "ilspy --version",
    "expect_success": true
  },
  "integration_test": {
    "command": "ilspy doctor",
    "expect_success": true,
    "description": "Verifies .NET runtime is present and the IlSpyBridge.dll loads successfully."
  },
  "skills": [
    "ilspy-cli"
  ],
  "agents": [
    "ilspy"
  ],
  "post_install_hints": [
    "Install .NET 8 SDK if missing (Windows: `winget install Microsoft.DotNet.SDK.8`; macOS: `brew install --cask dotnet-sdk`; Linux: see https://learn.microsoft.com/dotnet/core/install/linux).",
    "Stage the bridge DLL to a stable location: copy `<repo-clone>/ilspy-cli/target/release/bridge/` to e.g. `~/tools/ilspy-bridge`, then set `ILSPY_BRIDGE_DIR` to that path (persist with `setx` on Windows or shell rc on Unix).",
    "Run `ilspy doctor` — checks .NET runtime + bridge DLL load."
  ]
}
