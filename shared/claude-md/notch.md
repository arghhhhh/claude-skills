## Notch - Real-Time VFX Scene Authoring

When user wants to build or modify a Notch (notch.one) scene — create/wire/configure nodes, look up a node's properties or CreateNode string, debug a Notch JS script, or extend automation of Notch Builder 2026.1 — read `~/.claude/skills/notch/SKILL.md`.

The skill includes a CLI (`node ~/.claude/skills/notch/notch-node-info.js <NodeName>`) that pulls live property metadata from the Notch manual for any of 651 nodes, already prefixed with the mandatory `Category.PropertyName` form Notch's JS API requires.

Trigger phrases: "notch", "notch builder", ".dfx", "skybox", "video loader", "environment image", "javascript node", "nodegraph", "notch.one"
