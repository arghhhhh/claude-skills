## GitHub CLI (gh)

When working with GitHub issues, PRs, repos, or CI, read `~/.claude/skills/github-cli.md`.

Trigger phrases: "issue", "pull request", "PR", "gh", "GitHub", "repo", "CI checks", "release", "workflow run"
