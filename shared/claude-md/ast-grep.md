## ast-grep - Structural Code Search & Rewriting

When user needs to search for code patterns, find specific language constructs, or refactor code structurally, read `~/.claude/skills/ast-grep/SKILL.md`.

Trigger phrases: "find all", "search for pattern", "refactor", "replace all instances of", "code structure", "ast-grep", "find usage"
